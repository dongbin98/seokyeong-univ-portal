<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    android:paddingTop="4dp"
    android:paddingLeft="14dp"
    android:paddingRight="14dp"
    android:paddingBottom="4dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/attendance_spinner_text"
        android:layout_width="wrap_content"
        android:layout_height="20dp"
        android:textSize="16dp"
        android:textStyle="bold"
        android:textColor="@color/mainBlue"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <ImageView
        android:id="@+id/attendance_spinner_triangle"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:layout_marginLeft="2dp"
        android:src="@drawable/imageview_attendance_dropdown_triangle"
        app:layout_constraintTop_toTopOf="@id/attendance_spinner_text"
        app:layout_constraintLeft_toRightOf="@id/attendance_spinner_text"
        app:layout_constraintBottom_toBottomOf="@id/attendance_spinner_text"/>

</androidx.constraintlayout.widget.ConstraintLayout>